/**
 * FinishInsert.js
 * ModifyTimetable
 */

const express = require("express");
const router = express.Router();

router.post("/FinishInsert", (req, res) => {});

router.get("/ModifyTimetable", (req, res) => {});
module.exports = router;
